const { circleArea, circleCircumference } = require("./circle");

const myRadius = 5;
console.log(`Dairenin alanı: ${circleArea(myRadius)}`);
console.log(`Dairenin çevresi: ${circleCircumference(myRadius)}`);
